/* Test: static array initialization */

program array_static_init
let
    type intArray = array[3] of int;
    static a : intArray := 20;
begin

    function main() : int
    begin
        printi(a[0]);
        printi(a[1]);
        printi(a[2]);
        return 0;
    end

end
