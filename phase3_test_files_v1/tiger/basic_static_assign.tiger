/* Test: static integer assignment */

program basic_static_assign
let
    static a : int;
    static b, c  : int;
begin

    function main() : int
    begin
        a := 10;
        b := 20;
        c := 30;

        printi(a);
        printi(b);
        printi(c);
        return 0;
    end

end
