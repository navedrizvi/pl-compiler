/* Parse test: precedence of parenthesis */

program parse_precedence5
let
    static a: int;
begin

    function main() : int
    begin
        a := (((1 * 2) ** 2) ** 3);
        return 0;
    end

end
