/* Test: static integer initialization */

program basic_static_init
let
    static a : int := 40;
    static b , c  : int := 60;
begin

    function main() : int
    begin
        b := 50;

        printi(a);
        printi(b);
        printi(c);

        return 0;
    end

end
