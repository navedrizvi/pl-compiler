(cd submission-p2 && zip -r ../submission.zip .)


Just a text file w rough info. about building

$ javac Tiger*.java

// invoke with
$ java Tiger -i <path/to/file.tiger>

// tested with
$ java Tiger -i test.tiger
$ java Tiger -i test.tiger -l // print tokens
$ java Tiger -i test.tiger -p // write parse tree to dot format
